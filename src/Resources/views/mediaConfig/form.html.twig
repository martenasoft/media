{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form) }}


    {% set cancel_url = path('menu_admin_index') %}
    {% include '@MartenaSoftCommon/common/form_bottom.html.twig' %}
    {{ form_end(form) }}
{% endblock %}